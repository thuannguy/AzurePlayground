# ServiceBus

To use the sample, firstly, create a queue on Azure and copy its connection string. Secondly, create a secret in Key Vault whose name is "ServiceBusConnectionString
" and value is the connection string.